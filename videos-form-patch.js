async function enrichYoutubeForm(form){const id=parseYoutubeVideoId(form.elements.url.value);if(!id)return;if(form.elements.url.value.includes('/shorts/'))form.elements.type.value='short';const feedback=form.querySelector('[data-youtube-feedback]');feedback.textContent='Buscando título, canal e miniatura no YouTube...';try{const url=`https://www.youtube.com/watch?v=${id}`;const response=await fetch(`https://www.youtube.com/oembed?url=${encodeURIComponent(url)}&format=json`);if(!response.ok)throw new Error();const metadata=await response.json();form.elements.title.value=metadata.title||form.elements.title.value;form.elements.channelName.value=metadata.author_name||form.elements.channelName.value;form.elements.thumbnail.value=metadata.thumbnail_url||`https://i.ytimg.com/vi/${id}/hqdefault.jpg`;feedback.textContent=`Vídeo identificado: ${metadata.title||id}`;}catch{feedback.textContent='O link foi identificado. Confira o título, o canal e a duração antes de salvar.';}}
function openVideoForm(video=null){const channels=getChannels();const categories=getTaxonomyValues('categories');const playlists=getTaxonomyValues('playlists');const currentChannel=channels.find(c=>c.key===video?.channel);const data=video||{type:'long',duration:'',category:'',playlist:'',tags:[],notes:'',favorite:false,watched:false};openModal(`<span class="eyebrow">Administração</span><h2>${video?'Editar vídeo':'Adicionar vídeo'}</h2><form id="videoCrudForm" class="crud-form"><label class="crud-field full">Link do YouTube<input name="url" type="url" required placeholder="https://youtu.be/..." value="${escapeHtml(video?getVideoUrl(data):'')}"></label><div class="crud-feedback" data-youtube-feedback>Ao colar o link, o portal preenche título, canal e miniatura automaticamente.</div><label class="crud-field full">Título<input name="title" required value="${escapeHtml(data.title||'')}"></label><label class="crud-field">Canal<input name="channelName" list="videoChannelOptions" required placeholder="Selecione ou digite o canal" value="${escapeHtml(currentChannel?.title||'')}"><datalist id="videoChannelOptions">${channels.map(c=>`<option value="${escapeHtml(c.title)}">`).join('')}</datalist></label><label class="crud-field">Tipo<select name="type"><option value="long" ${data.type==='long'?'selected':''}>Vídeo completo</option><option value="short" ${data.type==='short'?'selected':''}>Short</option></select></label><label class="crud-field">Duração<input name="duration" placeholder="12:34" value="${escapeHtml(data.duration||'')}"></label><label class="crud-field">Categoria<input name="category" list="videoCategoryOptions" value="${escapeHtml(data.category||'')}"><datalist id="videoCategoryOptions">${categories.map(n=>`<option value="${escapeHtml(n)}">`).join('')}</datalist></label><label class="crud-field">Playlist<input name="playlist" list="videoPlaylistOptions" value="${escapeHtml(data.playlist||'')}"><datalist id="videoPlaylistOptions">${playlists.map(n=>`<option value="${escapeHtml(n)}">`).join('')}</datalist></label><label class="crud-field full">Tags separadas por vírgula<input name="tags" value="${escapeHtml((data.tags||[]).join(', '))}"></label><label class="crud-field full">Observações<textarea name="notes" rows="4">${escapeHtml(data.notes||'')}</textarea></label><input name="thumbnail" type="hidden" value="${escapeHtml(data.thumbnail||'')}"><label class="crud-check"><input name="favorite" type="checkbox" ${data.favorite?'checked':''}> Favorito</label><label class="crud-check"><input name="watched" type="checkbox" ${data.watched?'checked':''}> Assistido</label><div class="crud-actions full"><button class="ghost" type="button" data-video-modal-close-alt>Cancelar</button><button class="primary" type="submit">Salvar vídeo</button></div></form>`);const form=document.querySelector('#videoCrudForm');form.elements.url.addEventListener('change',()=>enrichYoutubeForm(form));form.elements.url.addEventListener('paste',()=>setTimeout(()=>enrichYoutubeForm(form),0));form.querySelector('[data-video-modal-close-alt]').addEventListener('click',closeModal);form.addEventListener('submit',async event=>{event.preventDefault();const submit=form.querySelector('[type="submit"]');submit.disabled=true;try{requireAdmin();const values=new FormData(form);const id=parseYoutubeVideoId(values.get('url'));if(!id)throw new Error('O link informado não contém um ID válido do YouTube.');const channelName=String(values.get('channelName')||'').trim();if(!channelName)throw new Error('Informe o canal do vídeo.');let channel=getChannels().find(c=>c.title?.toLowerCase()===channelName.toLowerCase()||c.handle?.toLowerCase()===channelName.toLowerCase()||c.key===slugify(channelName));if(!channel){const key=slugify(channelName);channel={key,title:channelName,handle:'',url:'',thumbnail:''};await state.sdk.setDoc(state.sdk.doc(state.db,'channels',key),{...channel,deleted:false,createdAt:state.sdk.serverTimestamp(),updatedAt:state.sdk.serverTimestamp()},{merge:true});}const payload={id,url:getVideoUrl({id,type:values.get('type'),url:values.get('url')}),title:String(values.get('title')||'').trim(),channel:channel.key,type:values.get('type')==='short'?'short':'long',date:video?.date||'',duration:String(values.get('duration')||'').trim()||'—',views:Number(video?.views||0),category:String(values.get('category')||'').trim(),playlist:String(values.get('playlist')||'').trim(),tags:String(values.get('tags')||'').split(',').map(t=>t.trim()).filter(Boolean),notes:String(values.get('notes')||'').trim(),thumbnail:String(values.get('thumbnail')||'').trim()||`https://i.ytimg.com/vi/${id}/hqdefault.jpg`,favorite:form.elements.favorite.checked,watched:form.elements.watched.checked,deleted:false,updatedAt:state.sdk.serverTimestamp()};if(!video)payload.createdAt=state.sdk.serverTimestamp();await state.sdk.setDoc(state.sdk.doc(state.db,'videos',id),payload,{merge:true});if(video&&video.id!==id)await state.sdk.setDoc(state.sdk.doc(state.db,'videos',video.id),{id:video.id,deleted:true,updatedAt:state.sdk.serverTimestamp()},{merge:true});closeModal();}catch(error){alert(error.message||'Não foi possível salvar o vídeo.');}finally{submit.disabled=false;}});}
